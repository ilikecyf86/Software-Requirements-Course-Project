Sometimes after deletion of git repository IDE still consider that project under git
kind/bug severity/P2 team/plugin 
Description In some cases after deletion of repository from project, Che still consider, that the project has a git repository. Git menu actions is active, but will fail because no repository actually present. The problem is gone after page refreshing. Reproduction Steps TODO add OS and version: Fedora 25, Che 6