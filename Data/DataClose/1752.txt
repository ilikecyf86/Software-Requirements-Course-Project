eclipse 'bin' in gitignore ignores nessacery path of che/assembly/assembly-main/src/assembly/bin/
kind/enhancement team/platform severity/P2 sprint/next-sprint status/open-for-dev sprint/current-sprint sprint/next-sprint status/open-for-dev sprint/current-sprint 
Not sure if this is a correct workflow for che. Cloning the project. Checking out the desired version tag. ex 5.0.0. Removing .git folder in order to start fresh without all the history commits. Git add all. Here the git add is skipping the che/assembly/assembly-main/src/assembly/bin/ path. That is because the main gitignore includes bin as a directory to igonore in all level (eclipse). https://github.com/eclipse/che/blob/master/.gitignore#L9 I the desire is to ignore eclipse generated bin folders then maybe there could be a .gitignore in the assembly-main/src/assembly/ path that include a negate line for the bin folder !bin/