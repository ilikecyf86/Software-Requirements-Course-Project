Add info about known Che issues into selenium tests DirectUrlFactoryWithSpecificBranch and of testrunner package
team/production kind/task 
Description We have some specific selenium tests that may fail testing different functionality but the reason they fail is the same. So we should catch this typical paces of falling and complete this one assertions with depended issue. We should implement this one for test from testrunner package and DirectUrlFactoryWithSpecificBranch test