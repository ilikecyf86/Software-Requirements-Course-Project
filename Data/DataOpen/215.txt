IDE memory consumption grows and the tab becomes crashed
kind/bug target/che6 
Description After using of IDE and leaving it for some time opened, the tab with IDE becomes crashed and the memory consumption is continuously increasing. In my case, there is no any huge process in IDE (like maven build), there is only imported and configured Che project, three open files and one terminal tab. Environment: single user Che branch che6 with the following state https://github.com/eclipse/che/tree/6d06d10273 A workspace with 1 machine(6gb RAM) that is created from Default Java stack with JDK 8, Maven and Tomcat. Reproduction Steps Open IDE go and leave it for a while Expected: IDE works as well Actual: the IDE tab is crashed. Take a look at the following screenshot