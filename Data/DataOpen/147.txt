Add an animated loader (bars) in the command controller
kind/enhancement sprint/next-sprint status/open-for-dev team/plugin sprint/current-sprint sprint/next-sprint 
Add an animated loader (bars) in the command controller (instead of “LOAD”) (the one we have on dashboard)